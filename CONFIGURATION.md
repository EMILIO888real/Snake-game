# Configuration

The game can be customized through a variety of configuration options that control aspects such as gameplay mechanics, visual presentation, audio settings, and performance monitoring. Below is a comprehensive list of these options along with their default values:

1. **sector size**: Grid cell dimensions in pixels [width, height] (default: `[40, 40]`)
2. **step**: Movement increment per update cycle (default: `4`)
3. **increase jumps with increasing ups**: Whether jumps increase with UPS changes (default: `false`)
4. **background color**: RGB background color value (default: `[30, 30, 30]`)
5. **use projects fonts**: Enable custom project fonts (default: `true`)
6. **font name**: Font typeface filename (default: `"Hoshiko Satsuki.ttf"`)
7. **font size**: Text display size in pixels (default: `23`)
8. **font bold**: Font weight toggle (default: `null`)
9. **font italic**: Font style toggle (default: `null`)
10. **window name**: Application window title (default: `"snake game!"`)
11. **lost text**: Game over message text (default: `"Everyone crashed!"`)
12. **lost text color**: RGB color for loss message (default: `[255, 0, 0]`)
13. **lost text font**: Custom font for loss text (default: `null`)
14. **lost text font size**: Font size for loss text (default: `null`)
15. **lost text bold**: Bold font weight for loss text (default: `null`)
16. **lost text italic**: Italic style for loss text (default: `null`)
17. **quit text**: Quit game message (default: `"You quit!"`)
18. **quit text color**: RGB color for quit message (default: `[0, 255, 255]`)
19. **quit text font**: Custom font for quit text (default: `null`)
20. **quit text font size**: Font size for quit text (default: `null`)
21. **quit text bold**: Bold weight for quit text (default: `null`)
22. **quit text italic**: Italic style for quit text (default: `null`)
23. **bye text position**: End screen text position normalized [x, y] (default: `[0.5, 0.1]`)
24. **end screen points text**: Points label text (default: `"points: "`)
25. **end screen points text variable index**: Variable index in points text (default: `8`)
26. **end screen points text color**: RGB color for points text (default: `[127, 0, 255]`)
27. **end screen points text font**: Custom font for points text (default: `null`)
28. **end screen points text font size**: Font size for points text (default: `null`)
29. **end screen points text bold**: Bold weight for points text (default: `null`)
30. **end screen points text italic**: Italic style for points text (default: `null`)
31. **end screen points text position**: Points text position normalized [x, y] (default: `[0.5, 0.15]`)
32. **end screen high score text**: High score label text (default: `"high score: "`)
33. **end screen high score text variable index**: Variable index in high score text (default: `12`)
34. **end screen high score text color**: RGB color for high score text (default: `[255, 127, 0]`)
35. **end screen high score text font**: Custom font for high score text (default: `null`)
36. **end screen high score text font size**: Font size for high score text (default: `null`)
37. **end screen high score text bold**: Bold weight for high score text (default: `null`)
38. **end screen high score text italic**: Italic style for high score text (default: `null`)
39. **end screen high score text position**: High score text position normalized [x, y] (default: `[0.5, 0.2]`)
40. **points text**: In-game points label text (default: `"points: "`)
41. **points text variable index**: Variable index in points text (default: `8`)
42. **points text color**: RGB color for in-game points text (default: `[200, 200, 200]`)
43. **points text font**: Custom font for points text (default: `null`)
44. **points text font size**: Font size for points text (default: `null`)
45. **points text bold**: Bold weight for points text (default: `null`)
46. **points text italic**: Italic style for points text (default: `null`)
47. **points text position**: In-game points text position normalized [x, y] (default: `[0.9, 0.05]`)
48. **start menu text**: Start menu instruction text (default: `"Press any of the movement(WASD) keys to start"`)
49. **start menu text color**: RGB color for start menu text (default: `[255, 255, 255]`)
50. **start menu text font**: Custom font for start menu text (default: `null`)
51. **start menu text font size**: Font size for start menu text (default: `null`)
52. **start menu text bold**: Bold weight for start menu text (default: `null`)
53. **start menu text italic**: Italic style for start menu text (default: `null`)
54. **start menu text position**: Start menu text position normalized [x, y] (default: `[0.5, 0.45]`)
55. **paused text**: Pause screen message (default: `"Game paused"`)
56. **paused text color**: RGB color for pause text (default: `[255, 255, 0]`)
57. **paused text font**: Custom font for pause text (default: `null`)
58. **paused text font size**: Font size for pause text (default: `null`)
59. **paused text bold**: Bold weight for pause text (default: `null`)
60. **paused text italic**: Italic style for pause text (default: `null`)
61. **paused text position**: Pause text position normalized [x, y] (default: `[0.5, 0.45]`)
62. **performance text**: FPS/UPS display format text (default: `"fps/ups: [\\]"`)
63. **performance text variable fps index**: Variable index for FPS (default: `10`)
64. **performance text variable ups index**: Variable index for UPS (default: `11`)
65. **performance text color**: RGB color for performance text (default: `[255, 255, 255]`)
66. **performance text font**: Custom font for performance text (default: `null`)
67. **performance text font size**: Font size for performance text (default: `"-5"`)
68. **performance text bold**: Bold weight for performance text (default: `null`)
69. **performance text italic**: Italic style for performance text (default: `null`)
70. **performance text position**: Performance text position normalized [x, y] (default: `[0.001, 0.001]`)
71. **music notification text**: Now playing notification text (default: `"Playing  "`)
72. **music notification text color**: RGB color for music notification (default: `[255, 255, 255]`)
73. **music notification text font**: Custom font for music notification (default: `null`)
74. **music notification text font size**: Font size for music notification (default: `14`)
75. **music notification text bold**: Bold weight for music notification (default: `null`)
76. **music notification text italic**: Italic style for music notification (default: `null`)
77. **music notification text position**: Music notification position normalized [x, y] (default: `[0.5, 0.9]`)
78. **music notification variable index**: Variable index in music notification (default: `8`)
79. **volume notification text**: Volume display format (default: `"%"`)
80. **volume notification text color**: RGB color for volume notification (default: `[255, 255, 255]`)
81. **volume notification text font**: Custom font for volume notification (default: `null`)
82. **volume notification text font size**: Font size for volume notification (default: `null`)
83. **volume notification text bold**: Bold weight for volume notification (default: `null`)
84. **volume notification text italic**: Italic style for volume notification (default: `null`)
85. **volume notification text position**: Volume notification position normalized [x, y] (default: `[0.9, 0.5]`)
86. **volume notification text variable index**: Variable index in volume notification (default: `0`)
87. **snake bot delay**: AI snake decision delay in seconds (default: `0.0`)
88. **collision between snakes**: Enable snake-to-snake collision (default: `true`)
89. **eating sfx**: Sound effect for eating food (default: `"eating.mp3"`)
90. **lose sfx**: Sound effect for losing game (default: `"random"`)
91. **music fade out**: Music fade-out duration in milliseconds (default: `2000`)
92. **sequential starting position**: Use sequential spawn positions for snakes (default: `false`)
93. **careful between snakes collision x portion**: X-axis collision detection width ratio (default: `0.5`)
94. **careful between snakes collision y portion**: Y-axis collision detection height ratio (default: `0.5`)
95. **volume up increment**: Volume increase per key press (default: `0.05`)
96. **volume down increment**: Volume decrease per key press (default: `0.05`)
97. **playlist cycle time**: Playlist update cycle time in seconds (default: `0.1`)
98. **notification display time**: Notification display duration in seconds (default: `3.0`)
99. **music notification display time**: Music notification display duration (default: `null`)
100. **volume notification display time**: Volume notification display duration (default: `null`)
101. **notification cycle time**: Notification animation cycle time (default: `0.05`)
102. **notification waiting cycle time**: Notification wait state cycle time (default: `0.1`)
103. **snake head width**: Snake head rendering width in pixels (default: `8`)
104. **food width**: Food rendering width in pixels (default: `10`)
105. **grid lines width**: Grid line rendering width (default: `1`)
106. **good fps color**: RGB color for good FPS threshold (default: `[0, 255, 0]`)
107. **normal fps color**: RGB color for normal FPS threshold (default: `[255, 255, 0]`)
108. **bad fps color**: RGB color for bad FPS threshold (default: `[255, 0, 0]`)
109. **good ups color**: RGB color for good UPS threshold (default: `[0, 255, 0]`)
110. **normal ups color**: RGB color for normal UPS threshold (default: `[255, 255, 0]`)
111. **bad ups color**: RGB color for bad UPS threshold (default: `[255, 0, 0]`)
112. **good fps**: FPS threshold for good performance ratio (default: `0.9`)
113. **normal fps**: FPS threshold for normal performance ratio (default: `0.7`)
114. **good ups**: UPS threshold for good performance ratio (default: `0.95`)
115. **normal ups**: UPS threshold for normal performance ratio (default: `0.9`)
116. **skip start menu**: Skip intro screen on startup (default: `false`)
117. **start direction**: Initial snake direction code (default: `4`)
118. **extra performance text fps spacing**: Additional spacing for FPS text (default: `0`)
119. **extra performance text ups spacing**: Additional spacing for UPS text (default: `0`)
120. **slow key input cycle time**: Slow input handling cycle time (default: `0.035`)
121. **hold key increment**: Hold key repeat increment (default: `3`)
122. **hold key minimal value**: Minimum hold time before repeat (default: `10`)
123. **clamp master volume**: Clamp volume to valid range (default: `true`)
124. **master volume range**: Volume min/max bounds (default: `[0.0, 1.0]`)
125. **key inputs cycle time**: Key input processing cycle time (default: `0.03`)
126. **snake pause cycle time**: Snake pause state cycle time (default: `0.001`)
127. **skip end screen**: Skip end screen after game over (default: `false`)
128. **serialize data**: Save game data to file (default: `false`)
129. **faster food position**: Optimize food position calculation (default: `true`)
130. **very high ups performance stats**: Display high-precision UPS stats (default: `false`)
131. **round performance stats**: Round performance display values (default: `false`)
132. **fps round decimal places**: Decimal places for FPS rounding (default: `1`)
133. **ups round decimal places**: Decimal places for UPS rounding (default: `1`)
134. **log**: Enable debug logging (default: `false`)
135. **reset log**: Clear log on startup (default: `true`)
136. **relative log time**: Use relative timestamps in logs (default: `true`)
137. **log text for new session**: Session start log message (default: `"=============== SESSION START [] ==============="`)
138. **log new session text time var index**: Variable index for session time (default: `31`)
139. **show last frame on end screen**: Display final game frame (default: `false`)
140. **show last frame on end screen snake index**: Snake index to highlight (default: `-1`)
141. **custom error message**: Enable custom error messages (default: `true`)
142. **generate error report**: Generate error reports on errors (default: `true`)
143. **overwrite error reports**: Overwrite existing error reports (default: `false`)
144. **music directories**: List of directories for music files (default: `["./.music/"]`)
145. **busy loop threshold**: Threshold for busy loop detection (default: `0.0001`)
146. **disable key inputs**: Disable all key inputs (default: `false`)
147. **wait for bot**: Wait for bot actions before proceeding (default: `false`)
148. **error report text start**: Error report start marker (default: `"\n_______________ERROR REPORT START_______________\n"`)
149. **error report text version**: Error report version label (default: `"version: "`)
150. **error report text version variable index**: Variable index for version (default: `9`)
151. **error report text date**: Error report date label (default: `"date: "`)
152. **error report text date variable index**: Variable index for date (default: `7`)
153. **error report text platform**: Error report platform label (default: `"platform: "`)
154. **error report text platform variable index**: Variable index for platform (default: `10`)
155. **error report text integrity**: Error report integrity label (default: `"integrity check: "`)
156. **error report text integrity variable index**: Variable index for integrity check (default: `17`)
157. **error report text integrity successful**: Integrity check success message (default: `"successful [correct directory]"`)
158. **error report text integrity failed**: Integrity check failure message (default: `"failed [missing files: ]"`)
159. **error report text integrity failed variable index**: Variable index for missing files (default: `25`)
160. **time text**: Time display text (default: `""`)
161. **time text variable index**: Variable index for time (default: `0`)
162. **time text color**: RGB color for time text (default: `[255, 255, 255]`)
163. **time text font**: Font for time text (default: `"CascadiaCode-Regular.ttf"`)
164. **time text font size**: Font size for time text (default: `null`)
165. **time text bold**: Bold weight for time text (default: `null`)
166. **time text italic**: Italic style for time text (default: `null`)
167. **time text position**: Time text position normalized [x, y] (default: `[0.5, 0.05]`)
168. **rating points multiplier**: Points multiplier for rating calculation (default: `1`)
169. **rating play time multiplier**: Play time multiplier for rating calculation (default: `1`)
170. **rating points portion**: Portion of rating from points (default: `0.8`)
171. **rating play time portion**: Portion of rating from play time (default: `0.2`)
172. **end screen rating text**: End screen rating label (default: `"rating: "`)
173. **end screen rating text variable index**: Variable index for rating (default: `8`)
174. **end screen rating text color**: RGB color for rating text (default: `[255, 255, 0]`)
175. **end screen rating text font**: Custom font for rating text (default: `null`)
176. **end screen rating text font size**: Font size for rating text (default: `null`)
177. **end screen rating text bold**: Bold weight for rating text (default: `null`)
178. **end screen rating text italic**: Italic style for rating text (default: `null`)
179. **end screen rating text position**: Rating text position normalized [x, y] (default: `[0.5, 0.25]`)
180. **end screen best rating text**: End screen best rating label (default: `"best rating: "`)
181. **end screen best rating text variable index**: Variable index for best rating (default: `13`)
182. **end screen best rating text color**: RGB color for best rating text (default: `[255, 127, 255]`)
183. **end screen best rating text font**: Custom font for best rating text (default: `null`)
184. **end screen best rating text font size**: Font size for best rating text (default: `null`)
185. **end screen best rating text bold**: Bold weight for best rating text (default: `null`)
186. **end screen best rating text italic**: Italic style for best rating text (default: `null`)
187. **end screen best rating text position**: Best rating text position normalized [x, y] (default: `[0.5, 0.3]`)
188. **repeat song**: Repeat current song in playlist (default: `false`)
189. **performance stats cycle time**: Cycle time for updating performance stats (default: `0.2`)
190. **show time format**: Format string for time display (default: `"%M:%S.%m"`)
191. **show time cycle time**: Cycle time for updating time display (default: `0.02`)
192. **soft restart**: Enable soft restart of game (default: `false`)
193. **error report text python version**: Python version label in error report (default: `"python version: "`)
194. **error report text python version variable index**: Variable index for python version (default: `16`)
195. **error report text pygame version**: Pygame version label in error report (default: `"pygame version: "`)
196. **error report text pygame version variable index**: Variable index for pygame version (default: `16`)
197. **error report text sdl version**: SDL version label in error report (default: `"sdl version: "`)
198. **error report text sdl version variable index**: Variable index for SDL version (default: `13`)
199. **version text**: Version label for display (default: `"version: "`)
200. **version text variable index**: Variable index for version text (default: `9`)
201. **version text color**: RGB color for version text (default: `[255, 255, 255]`)
202. **version text font**: Custom font for version text (default: `null`)
203. **version text font size**: Font size for version text (default: `"-10"`)
204. **version text bold**: Bold weight for version text (default: `null`)
205. **version text italic**: Italic style for version text (default: `null`)
206. **version text position**: Version text position normalized [x, y] (default: `[0.08, 0.98]`)